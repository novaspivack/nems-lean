import FTLConstraints.Core.CorrelationVsSignal
import FTLConstraints.Theorems.NoCompiler
import FTLConstraints.Examples.ToyEPR

/-!
# FTLConstraints — Paper 47

No Spooky-to-Signal Compiler: Why EPR Correlations Cannot be Total-Effectively Upgraded to FTL.

Under PSC and diagonal capability, no total-effective procedure can convert nonlocal
correlation into controllable signal; any such compiler would be an internal oracle (barred).
Schema + toy witness (parametric in hFP).
-/
